import { TitlecaseEsPipe } from './titlecase-es-pipe';

describe('TitlecaseEsPipe', () => {
  it('create an instance', () => {
    const pipe = new TitlecaseEsPipe();
    expect(pipe).toBeTruthy();
  });
});
